<!-- Notification List Modal -->
<div class="modal fade" id="notificationListModal" tabindex="-1" role="dialog" aria-labelledby="notificationListModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="notificationListModalLabel">Notifications</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body p-0" id="notificationListContainer" style="max-height: 60vh; overflow-y: auto;">
                <!-- Content loaded via AJAX -->
            </div>
            <div class="modal-footer justify-content-between">
                <button type="button" class="btn btn-outline-primary btn-sm" id="markAllReadBtn">Mark all as read</button>
                <button type="button" class="btn btn-secondary btn-sm" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

<!-- Notification Detail Modal -->
<div class="modal fade" id="notificationDetailModal" tabindex="-1" role="dialog" aria-labelledby="notificationDetailModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="notificationDetailModalLabel">Notification Details</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body" id="notificationModalBody">
                <div class="text-center py-3">
                    <div class="spinner-border text-primary" role="status">
                        <span class="sr-only">Loading...</span>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

<style>
    /* Ensure the notification link is positioned relatively for absolute badge positioning */
    .nav-link.position-relative {
        position: relative !important;
        display: inline-flex;
        align-items: center;
        justify-content: center;
    }
    
    /* Make the bell icon larger */
    .nav-link.position-relative .fa-bell {
        font-size: 1.5rem !important;
    }
    
    #notificationBadge {
        position: absolute !important;
        top: -5px !important;
        right: -5px !important;
        font-size: 0.7rem;
        padding: 3px 6px;
        min-width: 20px;
        height: 20px;
        line-height: 14px;
        text-align: center;
        border-radius: 10px;
        z-index: 10;
    }
    
    .notification-item {
        cursor: pointer;
        transition: background-color 0.2s;
    }
    
    .notification-item:hover {
        background-color: #f8f9fa;
    }
</style>
